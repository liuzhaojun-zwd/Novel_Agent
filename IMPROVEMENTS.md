# Novel_Agent — 改进清单

> 日期：2026-06-06

---

## 🔴 高优先级（严重影响体验或可用性）

### 1. 单章字数上限不足（max_tokens = 4096 → 需提升）
- 当前 `max_tokens=4096`，中文实际输出约 2000-3000 字
- `words_per_chapter` 可配高达 20000 字，差距 10 倍
- 方案：调高 max_tokens（建议 8192 +），或实现分段生成
- 状态：✅ 已完成

### 2. 无 LLM 流式输出到前端
- 当前等整章生成完才推送 `chapter_complete`，用户对黑白屏干等
- 应通过 SSE 流式推送正在生成的文字，用户看着文字实时冒出来
- 方案：LLM stream mode → publish tokens → 前端实时渲染
- 状态：⏳ 进行中

### 3. 上下文窗口管理是盲区
- `previous_summary_parts[-5:]` 硬编码取最后 5 章摘要，无 token 计算
- 长篇小说中超长前文摘要可能撑爆上下文窗口
- 方案：token 计数 + 动态选择回看章节 + 超窗压缩
- 状态：⏳ 进行中

### 4. 数据库连接管理裸奔
- 每个 `get_db()` 创建新连接，手动 `db.close()`
- 无连接池、异常时连接泄漏
- 方案：async context manager / 连接复用
- 状态：✅ 已完成

---

## 🟡 中优先级（功能缺口）

### 5. 一致性检查过于粗糙
- 基于姓氏+双字组合的规则匹配，大量误报且漏报
- 方案：引入 LLM 语义检测 / NER / 跨章节行为一致性

### 6. 无分段生成 / 章节内 checkpoint
- 超长章节（如 10000 字）若中途超时，整章内容丢失
- 方案：以段落/500字为粒度分段生成，每段立即持久化

### 7. 生成完成后不支持改稿
- 用户对某章不满意时无编辑能力
- 方案：选中某章提交修改指令 → 重新生成 → 连锁更新后续章节

### 8. 无写作质量评估机制
- 无法评价 AI 写得好不好
- 方案：每章质量评分（字数、情节密度、对话比例等）

---

## 🟢 低优先级（锦上添花）

### 9. 前端体验打磨不足
- 无 loading skeleton、暗色模式、滚动记忆、键盘快捷键

### 10. 无 LLM 调用缓存
- 相同设定重复生成大纲浪费 token

### 11. 无导入/导出创作设定
- 长篇种子设定难复用

### 12. 无多轮对话式创作
- 当前一次性设定→生成，缺少反馈→调整的 Agent 式交互